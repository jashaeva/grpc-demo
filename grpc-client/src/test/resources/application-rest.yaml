grpc:
  client:
    inventory-service:
      address: static://localhost:9090
      negotiation-type: plaintext
      max-inbound-message-size: 10MB

logging:
  level:
    io.grpc: DEBUG
    net.devh: DEBUG
