spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/grpc_demo
    username: postgres
    password: postgres
  flyway:
    schemas: inventory_schema
  kafka:
    bootstrap-servers: kafka:9092

grpc:
  server:
    port: 9090