rootProject.name = 'grpc-demo'

def modules = [
        'grpc-server',
        'grpc-client',
        'grpc-common',
        'e2e-tests'
]

modules.each { m ->
    if (file(m).exists()) {
        include m
    }
}
